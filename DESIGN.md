# 만나(Manna) 디자인 시스템

> 앱의 디자인 결정 사항을 기록합니다. AI 세션에서 자동으로 참조됩니다.
> 마지막 업데이트: 2026-04-15

---

## 디자인 철학

- **RPG 캐릭터 시트 컨셉** — 성경 읽기를 게임 캐릭터 성장처럼 시각화
- **집중 최우선** — 읽기 화면에서는 모든 UI 크롬을 제거, 말씀이 전부
- **골드 포인트 컬러** — 금색(#D4A847)은 완료·활성·강조에만 사용. 과하면 안 됨
- **다크 프리미엄** — 배경은 완전한 검정이 아닌 딥 네이비(#0B0A12). 따뜻한 느낌 유지

---

## 컬러 토큰

```
배경:       #0B0A12
서피스:     #13111F
서피스2:    #1C1A2E

골드:       #D4A847
골드라이트: #F0C96A
골드다크:   #B8882E
골드BG:    rgba(212,168,71,0.08)
골드보더:   rgba(212,168,71,0.18)

텍스트:     #FFFFFF
텍스트서브: rgba(255,255,255,0.55)
텍스트뮤트: rgba(255,255,255,0.32)
보더:       rgba(212,168,71,0.12)
보더서틀:   rgba(255,255,255,0.06)
```

---

## 아이콘 규칙

- **MaterialCommunityIcons 전용** — 다른 아이콘 라이브러리 사용 금지
- 이모지를 UI 요소로 사용 금지

---

## 폰트

- 본문 읽기: **나눔명조** (세리프, 성경 본문)
- UI 전반: 시스템 폰트 (Inter 계열)

---

## 컴포넌트 패턴

### 탭바 (Phone)
- 높이: 아이콘(26px) + 상하 여백 18씩 + bottomPad (동적)
- 활성: `#D4A847`, 비활성: `rgba(255,255,255,0.3)`
- 레이블: 없음 (아이콘만)
- 탭 항목 (표시): 홈 / 진행률 / 기록 / 기도 / 설정
- 탭 항목 (숨김): 검색(홈 검색바로 접근), 업적(진행률 '전체 보기'로 접근)

### 시트/모달
- 배경: `#13111F`
- 핸들 바: 상단 중앙 4x4px 라운드

### 글라스 플로팅 Pill (공용)
읽기 화면 구절 선택(VerseActionBar), 기도 화면 다중 선택 바 공통 스펙.
- 배경: `rgba(10,8,20,0.94)`, borderRadius 28
- 보더: `rgba(212,168,71,0.18)` 1px
- shadow: offset(0,8) / opacity 0.45 / radius 20 / elevation 14
- paddingVertical: 2px (pill), 12px (각 버튼)
- 구분선: 1px, height 28, `rgba(255,255,255,0.08)`
- **VerseActionBar**: 아이콘 22px (텍스트 라벨 없음), 기록 버튼만 골드 CTA pill
- **prayers 선택 바**: 아이콘 22px + 텍스트 라벨 10px/600 (세로 배치, gap 4)

---

## 태블릿(iPad) 레이아웃 — 2026-04-15 확정

### 내비게이션 패턴

| 방향 | 패턴 |
|---|---|
| 가로 (Landscape) | 사이드바 고정 노출 (220px) |
| 세로 (Portrait) | 사이드바 접힘, ☰ 버튼으로 열기/닫기 |
| 읽기 화면 | 방향 무관 사이드바 완전 숨김 |

### 사이드바 스펙

- 너비: 220px
- 배경: `#0B0A12`, 우측 보더: `rgba(212,168,71,0.12)` 1px
- 로고 영역: 높이 64px, 수직 중앙 정렬
- 내비 항목: 홈 / 진행률 / 기록 / 기도 / 설정
- 항목 높이: 52px, 좌측 패딩 20px, 아이콘(20px) + 텍스트 간격 12px
- 활성 항목: 골드(`#D4A847`) 왼쪽 보더 3px + 골드 텍스트
- 비활성 항목: `rgba(255,255,255,0.55)`
- Portrait 드로어: 열릴 때 오른쪽 콘텐츠에 `rgba(0,0,0,0.5)` 오버레이 (모달과 동일)

### 화면별 레이아웃

#### 홈
- **2컬럼 구조 (45 / 55 비율)**
- 1순위: RPG 캐릭터 존 (왼쪽) — XP 링이 가장 크고 눈에 띄어야 함
- 2순위: 계속 읽기 버튼 (오른쪽 상단) — 즉시 행동 유도
- 3순위: 주간 도트 + 오늘의 추천 구절 (오른쪽 하단)
- 왼쪽(45%): XP 링 + 레벨 + 스트릭 + 대표 배지
- 오른쪽(55%): 읽기 계획 진행률 + 주간 도트 + 계속 읽기 버튼 + 오늘의 추천 구절

#### 진행률
- **마스터-디테일 + 전체 상단 헤더**
- 상단 헤더 (사이드바 옆, 콘텐츠 전체 너비): 완독% · 구절 수 · 일수 통계 + 배지 그리드 + 스트릭 히트맵
- 왼쪽: 66권 그리드 (6~7열, 구약/신약 섹션 구분, 완독 권은 골드 강조)
- 오른쪽 패널: 선택한 권의 장 목록 (완독 여부 표시 + 장 탭 시 읽기 이동)
- 오른쪽 빈 상태 (책 미선택): book-open-variant 아이콘 + "왼쪽에서 권을 선택하세요"

#### 기록
- **마스터-디테일**
- 왼쪽 (35%): 검색바 + 필터 칩 + 묵상 목록
  - 선택 항목: 골드 왼쪽 보더
- 오른쪽 (65%): 선택된 묵상 전체 내용 + 편집/삭제/공유 버튼
  - 빈 상태: "항목을 선택하면 여기에 표시됩니다" + 미묘한 아이콘

#### 설정
- **마스터-디테일**
- 왼쪽 (30%): 카테고리 목록 (구독 / 텍스트 크기 / 알림 / 통독계획 / 데이터 / 앱정보)
  - 첫 항목 자동 선택
  - 선택 항목: 골드 왼쪽 보더
  - 알림: 서브항목으로 '읽기 알람' + '말씀 알림' 표시
- 오른쪽 (70%): 선택된 카테고리 콘텐츠

#### 기도
- **마스터-디테일**
- 왼쪽 (35%): 검색바 → 그룹 드롭다운 → 필터 탭(전체/미응답/응답) → 기도제목 목록
  - 그룹 드롭다운: 검색바 바로 아래, "전체 그룹 ▾" 형식, 탭 시 그룹 목록 표시
  - 선택 항목: 골드 왼쪽 보더
  - 목록 항목: 기도제목 제목 + 날짜 + 응답됨 배지(골드 체크)
  - 다중 선택 모드: 글라스 플로팅 pill (전체선택/공유/삭제/닫기)
- 오른쪽 (65%): 선택된 기도제목 상세
  - 상단: 제목(크게) + 응답 여부 토글 버튼 (기록과 시각 차별화 포인트)
  - 본문: 기도 내용 텍스트
  - 하단: 편집/삭제/공유 버튼 row
  - 빈 상태: "기도제목을 선택하면 여기에 표시됩니다" + hands-pray 아이콘

#### 읽기
- **몰입 모드** — 사이드바 없음
- 중앙 컬럼 고정 (max-width ~600px), 좌우 여백 자동
- 상단: 뒤로 버튼 / 책·장 이름 / 설정·공유
- 골드 진행 바 (상단)
- 구절 번호: 골드/뮤트, 본문: 나눔명조
- 하단: 이전 장 / 장 선택 / 다음 장
- TTS 플레이어: 하단 플로팅 필 형태

### 태블릿 빈 상태 / 로딩 패턴

| 위치 | 빈 상태 | 로딩 |
|---|---|---|
| 진행률 오른쪽 (책 미선택) | 중앙 book-open 아이콘 + "왼쪽에서 권을 선택하세요" 뮤트 텍스트 | skeleton 불필요 (즉시 렌더) |
| 기록 오른쪽 (항목 미선택) | 중앙 notebook 아이콘 + "항목을 선택하면 여기에 표시됩니다" | — |
| 기도 리스트 (기도제목 없음) | 중앙 hands-pray 아이콘 + "첫 기도제목을 등록해보세요" + 골드 + 버튼 | ActivityIndicator 중앙 |
| 기도 오른쪽 (항목 미선택) | 중앙 hands-pray 아이콘 + "기도제목을 선택하면 여기에 표시됩니다" | — |
| 홈 캐릭터 존 | 스켈레톤 링 + 회색 레벨/스트릭 자리 | ActivityIndicator |

### 구현 순서 (권장)

1. `useTabletLayout()` 훅 — breakpoint + orientation 감지
2. 사이드바 컴포넌트 — 가로 고정 / 세로 드로어
3. 읽기 화면 — max-width 제약 + 사이드바 숨김
4. 홈 2컬럼 레이아웃
5. 기록/진행률/설정/기도 마스터-디테일 패널
6. `app.json` → `supportsTablet: true` 변경

### 구현 주의사항

- **Portrait↔Landscape 전환** 시 선택된 항목(마스터-디테일 선택 상태) 유지 필요. 회전해도 오른쪽 패널 콘텐츠 사라지지 않아야 함. 사이드바 슬라이드 인/아웃: `Animated` 200ms ease-in-out
- **마스터-디테일 비율** — 고정 (사용자 드래그 조절 v1 미지원, 추후 검토)
- **기도/기록 FAB (+버튼)** — 마스터 패널(왼쪽) 하단 오른쪽 고정 플로팅. 폰의 탭바 위 FAB 위치와 동일 패턴 유지
- **설정 알림 서브항목** — '알림' 카테고리 탭 시 오른쪽 패널에 읽기 알람 + 말씀 알림 섹션 모두 표시 (스크롤). 왼쪽 목록에서 들여쓰기 없이 단일 항목으로 유지

### 승인된 모크업 경로

```
~/.gstack/projects/manna/designs/tablet-layout-20260415/
  variant-A.png          ← 홈 화면 (승인)
  progress-/variant-A.png ← 진행률
  meditations-/variant-A.png ← 기록
  settings-/variant-A.png ← 설정
  reading.png             ← 읽기 (몰입 모드)
```
